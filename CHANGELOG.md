# 变更日志

以时间倒序记录项目的每一次重要变动。

---

## [1.1.0] - 2026-08-12

### 新增

- Vercel 云端数据持久化支持：集成 Vercel Redis，问卷数据可长期保存。
- `api/_data.js` 自动检测 `REDIS_URL` 环境变量，优先使用原生 Redis 协议持久化。
- 同时兼容旧版 Vercel KV REST API，未配置 Redis/KV 时回退到内存数组。
- 安装 `redis` 依赖以支持原生 Redis URL 连接。
- `api/surveys.js`、`api/stats.js`、`api/export-csv.js` 适配异步加载，兼容 Redis 持久化。

### 文档

- 更新 `README.md` 数据存储说明，补充 Vercel Redis 配置步骤与 `REDIS_URL` 示例。

---

## [1.0.4] - 2026-08-12

### 变更

- 满意度调查问卷第 2 题移除"下面都是"选项，保留其余 8 个选项。

### 文档

- 同步更新 `功能介绍.md` 中第 2 题的选项说明。

---

## [1.0.3] - 2026-08-12

### 修复

- 修复 `start.bat` 因 UTF-8 无 BOM 编码导致中文乱码、窗口闪退的问题。
- 将 `timeout` 替换为更兼容的 `ping` 延迟，避免部分环境下输入重定向报错。
- 重新生成带 BOM 的 `start.bat`，确保 Windows 双击启动时中文正常显示。

### 新增

- 在项目根目录创建桌面快捷方式 `启动小程序预览.lnk`，双击可同时启动前后端服务。

---

## [1.0.2] - 2026-08-12

### 变更

- 调整满意度调查问卷第 3 题选项为：
  - 现在启动更换
  - 物业服务满两年后更换（2027.12.31）
  - 物业合同期满后更换（2028.9.30）
  - 不同意更换
- 调整满意度调查问卷第 2 题选项，新增"人员配备不足"和"下面都是"。

### 文档

- 同步更新 `功能介绍.md` 中的问卷选项说明。

---

## [1.0.1] - 2026-08-12

### 文档

- 更新 `功能介绍.md`，使其与当前源码保持一致。
- 补充满意度调查问卷各题的完整选项说明。
- 修正户号选择说明：楼栋固定为 14 栋，依次选择单元和房号。
- 补充工具箱中新增的相关资源和免责声明说明。

---

## [1.0.0] - 2026-08-12

### 新增

- 初始化项目：万科朗拾花语 · 6-5 地块物业服务小程序。
- 首页：小区动态展示与快捷入口导航。
- 满意度调查问卷：户号选择 + 7 题问卷提交。
- 工具箱：法律依据、流程说明与 FAQ。
- 底部 Tab 导航栏切换页面。
- 本地开发后端：Node.js HTTP 服务，端口 3002，支持户号白名单校验与问卷数据持久化。
- Vercel Serverless API： households / survey / surveys / stats / export-csv。
- 户号白名单管理：基于 `14栋.xlsx` 生成 152 个户号。

### 技术栈

- React 19 + TypeScript
- Vite 7
- Tailwind CSS 3.4
- React Router 7（HashRouter）
- Lucide React
- shadcn/ui
- Node.js HTTP（本地后端）
- Vercel Serverless Functions（云端后端）

---

## 变更日志格式说明

每条记录包含以下部分：

- **版本号**：遵循 [SemVer](https://semver.org/lang/zh-CN/) 规范，即 `主版本号.次版本号.修订号`。
- **日期**：变更发布的日期，格式为 `YYYY-MM-DD`。
- **分类**：
  - `新增`：新功能或新页面。
  - `修复`：Bug 修复。
  - `变更`：现有功能或行为的变更。
  - `优化`：性能、代码结构或体验优化。
  - `移除`：已移除的功能或文件。
  - `文档`：文档、注释或说明的更新。
  - `依赖`：依赖库的升级、降级或替换。
